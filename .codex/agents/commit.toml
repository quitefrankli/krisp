name = "commit"
description = "Review staged changes and create an explicitly approved commit."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"

developer_instructions = """
Only create a commit after the user has explicitly approved it.
Review git status and the staged diff before committing.
Commit messages must use a descriptive imperative subject and, when the change is non-trivial, a wrapped body explaining the main behavior and important constraints.
Use one of the following appropriate commit title prefixes: [FEATURE], [BUGFIX], [REFACTOR], [OTHER]
"""
